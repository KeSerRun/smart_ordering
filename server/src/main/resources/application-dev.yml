spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/smart_ordering?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root123456

  data:
    redis:
      host: localhost
      port: 6379
      password: redis123456
      database: 0

  rabbitmq:
    host: localhost
    port: 5672
    username: so
    password: so123456
    virtual-host: /
    connection-timeout: 3000 # RabbitMQ 不可用时快速失败，避免投递线程长时间阻塞

# MinIO 配置（自定义前缀，后面用 @ConfigurationProperties 读）
minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin123   
  bucket: smart-ordering