<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartordering.modules.system.mapper.SysMenuMapper">

    <!-- Query menus authorized to a user (user -> role -> menu join) -->
    <select id="selectMenusByUserId" resultType="com.smartordering.modules.system.entity.SysMenu">
        SELECT DISTINCT m.*
        FROM sys_user_role ur
                 JOIN sys_role_menu rm ON ur.role_id = rm.role_id
                 JOIN sys_menu m ON rm.menu_id = m.id
        WHERE ur.user_id = #{userId}
          AND m.deleted = 0
          AND m.status = 1
        ORDER BY m.order_num
    </select>
</mapper>